dotfiles
========

Installation
------------

 - Clone this repo somewhere (e.g. ~/Private/dotfiles).
 - Run `./deploy.sh` if you are on server machine or `./deploy.sh -a` if you want to install mutt, irssi and other
   "desktop" configuration files as well.
